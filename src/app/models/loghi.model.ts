export interface LoghiFooter {
  id: number;
  url: string; // Link al sito o profilo
  icon: string; // Percorso dell'immagine del logo
  altText?: string; // Testo alternativo per l'accessibilità
}
